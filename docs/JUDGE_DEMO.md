# Two-minute judge demo

## Hook — 0:00–0:15

“Web agencies often finish the work days before they get permission to invoice. The problem is not delivery—it is proving that every promise in the SOW is actually done.”

Open the workspace. Point to the signed SOW on the left and the six drafted checks on the right.

## Human-controlled AI — 0:15–0:40

“MilestoneProof extracts exact source quotes and maps only measurable promises into safe, typed checks. AI drafted these. A human decides what counts as done. It never generates or executes arbitrary test scripts.”

Click **Confirm all & run**.

## The visible reveal — 0:40–1:05

Wait for `launch-rc1`. Point at AC-04.

“The page showed a cheerful success message, so a normal visual review would pass it. But the underlying lead request returned HTTP 500. MilestoneProof caught the false success before the client—and before the invoice.”

Click **Verify fixed build**.

## Proof, not test jargon — 1:05–1:30

When `launch-rc2` passes, click **Create client review**, then **Open as the client**.

“The client does not get CI logs. They get the promises they signed, the outcome observed for each one, and one clear decision.”

Click **Approve milestone**, enter Mara Chen, and confirm.

## Business outcome — 1:30–1:55

Open the approval record.

“Now Northstar has an immutable record tying the SOW revision, build, evidence hashes, client, timestamp, and $12,000 milestone together. It is invoice-ready—not an invoice or legal signature—and it turns approval limbo into a three-minute workflow.”

## Close — 1:55–2:00

“MilestoneProof: give the client proof, not a test report.”

