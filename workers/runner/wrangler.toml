name = "greenlit-runner"
main = "src/index.ts"
compatibility_date = "2026-07-19"
compatibility_flags = ["nodejs_compat"]

[browser]
binding = "BROWSER"

[[queues.producers]]
binding = "JOB_QUEUE"
queue = "greenlit-jobs"

[[queues.consumers]]
queue = "greenlit-jobs"
max_batch_size = 1
max_batch_timeout = 1
max_concurrency = 1
max_retries = 1

[vars]
WEB_APP_URL = "https://greenlit.vercel.app"

# Secrets set with `wrangler secret put`:
# RUNNER_HMAC_SECRET
